type Box = {
  name: string,
  address: string,
  type: string,
  height: number,
  sensorHeight: number,
  isActive: boolean,
  battery: number,
  networkConnection: number,
  sleepTimeMinutes: number
  width: number,
  length: number,
};

export default Box;
